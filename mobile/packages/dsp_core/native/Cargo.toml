[package]
name = "dsp_core"
version = "0.1.0"
edition = "2021"
description = "AetherRide signal processing — filter, fusion, features (Spec §5.5)"

[lib]
crate-type = ["cdylib", "staticlib"]
name = "dsp_core"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
